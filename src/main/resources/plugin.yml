name: ${project.name}
main: ${project.groupId}.${project.artifactId}
version: ${project.version}
author: ${project.author}
api-version: 1.16
depend: [Vault]
libraries:
  - com.zaxxer:HikariCP:5.0.1
commands:
  collector:
    usage: /<command> <player> <amount>
    description: Give a player an amount of collectors